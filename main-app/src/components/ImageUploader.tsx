export default function ImageUploader({ onUploaded }: { onUploaded?: (url: string) => void }) {
  return (
    <div>
      <p>Single-image uploader UI will go here (presign - upload - return URL).</p>
    </div>
  )
}
